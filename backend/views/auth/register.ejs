<!doctype html>
<html lang="ko">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>회원가입</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;600;700;800&display=swap" rel="stylesheet">
  <style> body{font-family:'Noto Sans KR',system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial}</style>
</head>
<body>
  <main class="container" style="max-width: 520px;">
    <div class="py-5">
      <h1 class="h3">회원가입</h1>
      <% if (error) { %>
        <div class="alert alert-danger mt-3"><%= error %></div>
      <% } %>
      <form class="mt-3" action="/register" method="post">
        <div class="mb-3">
          <label class="form-label">아이디</label>
          <input name="username" class="form-control" required />
        </div>
        <div class="mb-3">
          <label class="form-label">비밀번호</label>
          <input type="password" name="password" minlength="6" class="form-control" required />
        </div>
        <div class="row g-3">
          <div class="col-12 col-sm-6">
            <label class="form-label">출생년월일</label>
            <input type="date" name="birthdate" class="form-control" />
          </div>
          <div class="col-12 col-sm-6">
            <label class="form-label">성별</label>
            <select name="gender" class="form-select">
              <option value="">선택 안 함</option>
              <option value="M">남</option>
              <option value="F">여</option>
            </select>
          </div>
        </div>
        <div class="row g-3 mt-1">
          <div class="col-12 col-sm-6">
            <label class="form-label">이메일</label>
            <input type="email" name="email" class="form-control" />
          </div>
          <div class="col-12 col-sm-6">
            <label class="form-label">전화번호</label>
            <input type="tel" name="phone" class="form-control" placeholder="010-0000-0000" />
          </div>
        </div>
        <button class="btn btn-primary w-100 mt-3">가입하기</button>
        <div class="text-center mt-3">
          <a href="/login">로그인</a> · <a href="/index.html">홈으로</a>
        </div>
      </form>
    </div>
  </main>
</body>
</html>
